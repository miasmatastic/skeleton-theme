{% comment %}
  🎬 Video Block - Universal video block with multiple sources
  
  Features:
  - Multiple video sources (upload, YouTube, Vimeo)
  - Responsive video embedding
  - Custom poster images
  - Autoplay and loop controls
  - Accessibility features
{% endcomment %}

{% liquid
  assign video_type = block.settings.video_type | default: 'upload'
  assign video_file = block.settings.video_file
  assign youtube_id = block.settings.youtube_id
  assign vimeo_id = block.settings.vimeo_id
  assign poster_image = block.settings.poster_image
  assign aspect_ratio = block.settings.aspect_ratio | default: '16/9'
  assign autoplay = block.settings.autoplay | default: false
  assign muted = block.settings.muted | default: true
  assign loop = block.settings.loop | default: false
  assign controls = block.settings.controls | default: true
  assign animation_type = block.settings.animation_type | default: 'none'
%}

<div class="block-video" 
     data-block-id="{{ block.id }}"
     data-video-type="{{ video_type }}"
     data-animation="{{ animation_type }}">
  
  <div class="video-container" 
       style="
         aspect-ratio: {{ aspect_ratio }};
         border-radius: {{ block.settings.border_radius | default: 0 }}px;
         overflow: hidden;
         position: relative;
       ">
    
    {% case video_type %}
      {% when 'upload' %}
        {% if video_file != blank %}
          <video 
            class="block-video-element"
            style="width: 100%; height: 100%; object-fit: cover;"
            {% if poster_image != blank %}poster="{{ poster_image | image_url: width: 1200 }}"{% endif %}
            {% if autoplay %}autoplay{% endif %}
            {% if muted %}muted{% endif %}
            {% if loop %}loop{% endif %}
            {% if controls %}controls{% endif %}
            preload="metadata"
            aria-label="{{ block.settings.video_title | default: 'Video content' | escape }}"
          >
            <source src="{{ video_file }}" type="video/mp4">
            <p>Your browser doesn't support video playback. <a href="{{ video_file }}">Download the video</a> instead.</p>
          </video>
        {% else %}
          {% if request.design_mode %}
            <div class="video-placeholder">
              <p>🎬 Upload a video file</p>
            </div>
          {% endif %}
        {% endif %}
      
      {% when 'youtube' %}
        {% if youtube_id != blank %}
          <iframe 
            class="block-video-element"
            src="https://www.youtube.com/embed/{{ youtube_id }}?{% if autoplay %}autoplay=1&{% endif %}{% if muted %}mute=1&{% endif %}{% if loop %}loop=1&playlist={{ youtube_id }}&{% endif %}{% unless controls %}controls=0&{% endunless %}rel=0&modestbranding=1"
            style="width: 100%; height: 100%; border: 0;"
            allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
            allowfullscreen
            title="{{ block.settings.video_title | default: 'YouTube video' | escape }}"
          ></iframe>
        {% else %}
          {% if request.design_mode %}
            <div class="video-placeholder">
              <p>🎬 Add YouTube video ID</p>
              <small>e.g., dQw4w9WgXcQ</small>
            </div>
          {% endif %}
        {% endif %}
      
      {% when 'vimeo' %}
        {% if vimeo_id != blank %}
          <iframe 
            class="block-video-element"
            src="https://player.vimeo.com/video/{{ vimeo_id }}?{% if autoplay %}autoplay=1&{% endif %}{% if muted %}muted=1&{% endif %}{% if loop %}loop=1&{% endif %}title=0&byline=0&portrait=0"
            style="width: 100%; height: 100%; border: 0;"
            allow="autoplay; fullscreen; picture-in-picture"
            allowfullscreen
            title="{{ block.settings.video_title | default: 'Vimeo video' | escape }}"
          ></iframe>
        {% else %}
          {% if request.design_mode %}
            <div class="video-placeholder">
              <p>🎬 Add Vimeo video ID</p>
              <small>e.g., 123456789</small>
            </div>
          {% endif %}
        {% endif %}
    {% endcase %}
    
    {% comment %} Video Overlay {% endcomment %}
    {% if block.settings.overlay_color != blank and block.settings.overlay_opacity > 0 %}
      <div class="video-overlay"
           style="
             position: absolute;
             top: 0;
             left: 0;
             width: 100%;
             height: 100%;
             background-color: {{ block.settings.overlay_color }};
             opacity: {{ block.settings.overlay_opacity | divided_by: 100.0 }};
             pointer-events: none;
           "></div>
    {% endif %}
  </div>
  
  {% comment %} Video Caption {% endcomment %}
  {% if block.settings.video_caption != blank %}
    <div class="video-caption" 
         style="
           margin-top: 0.5rem;
           font-size: 0.875rem;
           color: var(--color-base-text-secondary);
           text-align: center;
         ">
      {{ block.settings.video_caption }}
    </div>
  {% endif %}
</div>

<style>
  .video-placeholder {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    height: 100%;
    background: var(--color-base-border);
    border: 2px dashed var(--color-base-text-secondary);
    color: var(--color-base-text-secondary);
    text-align: center;
  }
</style>

{% schema %}
{
  "name": "🎬 Video Block",
  "settings": [
    {
      "type": "select",
      "id": "video_type",
      "label": "Video Source",
      "options": [
        {"value": "upload", "label": "📁 Upload File"},
        {"value": "youtube", "label": "📺 YouTube"},
        {"value": "vimeo", "label": "🎭 Vimeo"}
      ],
      "default": "upload"
    },
    {
      "type": "video",
      "id": "video_file",
      "label": "Video File",
      "info": "Upload an MP4 video file (if using Upload option)"
    },
    {
      "type": "text",
      "id": "youtube_id",
      "label": "YouTube Video ID",
      "placeholder": "dQw4w9WgXcQ",
      "info": "The ID from YouTube URL (youtube.com/watch?v=VIDEO_ID)"
    },
    {
      "type": "text",
      "id": "vimeo_id",
      "label": "Vimeo Video ID",
      "placeholder": "123456789",
      "info": "The ID from Vimeo URL (vimeo.com/VIDEO_ID)"
    },
    {
      "type": "text",
      "id": "video_title",
      "label": "Video Title (Accessibility)",
      "placeholder": "Describe the video content",
      "info": "Important for screen readers and SEO"
    },
    {
      "type": "header",
      "content": "📐 Video Settings"
    },
    {
      "type": "select",
      "id": "aspect_ratio",
      "label": "Aspect Ratio",
      "options": [
        {"value": "16/9", "label": "Widescreen (16:9)"},
        {"value": "4/3", "label": "Standard (4:3)"},
        {"value": "1/1", "label": "Square (1:1)"},
        {"value": "21/9", "label": "Ultrawide (21:9)"},
        {"value": "9/16", "label": "Vertical (9:16)"}
      ],
      "default": "16/9"
    },
    {
      "type": "image_picker",
      "id": "poster_image",
      "label": "Poster Image (Upload videos only)",
      "info": "Thumbnail image shown before video plays"
    },
    {
      "type": "range",
      "id": "border_radius",
      "min": 0,
      "max": 50,
      "step": 1,
      "unit": "px",
      "label": "Border Radius",
      "default": 0
    },
    {
      "type": "header",
      "content": "▶️ Playback Options"
    },
    {
      "type": "checkbox",
      "id": "autoplay",
      "label": "Autoplay",
      "default": false,
      "info": "Video starts playing automatically (may not work on mobile)"
    },
    {
      "type": "checkbox",
      "id": "muted",
      "label": "Muted",
      "default": true,
      "info": "Start video without sound (required for autoplay)"
    },
    {
      "type": "checkbox",
      "id": "loop",
      "label": "Loop",
      "default": false,
      "info": "Video repeats continuously"
    },
    {
      "type": "checkbox",
      "id": "controls",
      "label": "Show Controls",
      "default": true,
      "info": "Display play/pause and volume controls"
    },
    {
      "type": "header",
      "content": "🎨 Styling"
    },
    {
      "type": "color",
      "id": "overlay_color",
      "label": "Overlay Color",
      "info": "Add a color overlay on top of the video"
    },
    {
      "type": "range",
      "id": "overlay_opacity",
      "min": 0,
      "max": 100,
      "step": 5,
      "unit": "%",
      "label": "Overlay Opacity",
      "default": 0
    },
    {
      "type": "text",
      "id": "video_caption",
      "label": "Video Caption",
      "placeholder": "Caption or description for the video",
      "info": "Appears below the video"
    },
    {
      "type": "select",
      "id": "animation_type",
      "label": "Animation",
      "options": [
        {"value": "none", "label": "None"},
        {"value": "fade-in", "label": "Fade In"},
        {"value": "slide-up", "label": "Slide Up"},
        {"value": "scale-in", "label": "Scale In"}
      ],
      "default": "none"
    }
  ]
}
{% endschema %}